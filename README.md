# 蜗牛爬得慢 (ZJ Tech Blog)

这是一个基于 [VitePress](https://vitepress.dev/) 构建的技术博客项目，用于记录前沿前端、跨端开发以及 AI 结合的实践经验。

> **Site Title**: 蜗牛爬得慢  
> **Description**: 聚焦前沿前端、跨端与AI结合的实践记录

## 🛠️ 技术栈

- **Core**: [VitePress](https://vitepress.dev/) (Vue 3 + Vite)
- **Deployment**: GitHub Pages (via GitHub Actions)

## 📂 目录结构

```
zj-tech-blog/
├── docs/                   # 文档源文件目录
│   ├── .vitepress/         # VitePress 配置目录
│   │   └── config.mts      # 站点配置文件 (导航栏、侧边栏等)
│   ├── posts/              # 博客文章目录
│   │   ├── 2026-01-first-post.md
│   │   └── ...
│   ├── public/             # 静态资源目录 (图片等)
│   ├── index.md            # 首页布局配置
│   └── about.md            # 关于页面
├── .github/workflows/      # GitHub Actions 部署配置
└── package.json            # 项目依赖与脚本
```

## 🚀 快速开始

### 1. 安装依赖

确保本地已安装 Node.js (建议 v18+)。

```bash
npm install
```

### 2. 本地开发

启动本地开发服务器，实时预览修改。

```bash
npm run docs:dev
```

服务启动后访问: `http://localhost:5173/zj-tech-blog/` (具体端口看控制台输出)

### 3. 构建打包

构建生产环境的静态文件。

```bash
npm run docs:build
```

打包产物位于: `docs/.vitepress/dist`

### 4. 本地预览打包结果

```bash
npm run docs:preview
```

## ✍️ 如何写文章

1.  在 `docs/posts/` 目录下创建新的 `.md` 文件（例如 `my-new-post.md`）。
2.  使用 Markdown 语法编写内容。
3.  打开 `docs/.vitepress/config.mts`，在 `sidebar` 配置中添加新文章的链接，以便在侧边栏显示。

## 🚢 部署

本项目配置了 GitHub Actions 自动部署。
当代码 push 到 `main` 分支时，会自动触发构建并将生成的静态文件部署到 GitHub Pages。
